import type { Metadata } from "next";

export const metadata: Metadata = { title: "때샷 수정" };

export default function EditShotLayout({ children }: { children: React.ReactNode }) {
  return children;
}
