import type { Metadata } from "next";

export const metadata: Metadata = { title: "여행 수정" };

export default function EditTripLayout({ children }: { children: React.ReactNode }) {
  return children;
}
