# Design — 트립디토 (Trip Ditto)

**Locked design system · v6.38 (2026-08-20 확정)**

이후 모든 디자인·구현 작업(Hallmark 포함)은 이 파일을 먼저 읽고 따른다.
화면끼리는 **달라지는 것이 아니라 이 시스템을 공유해야 한다.** 바꿀 것이 있으면
개별 화면에서 오버라이드하지 말고 이 파일을 먼저 수정한다.

- 근거: 토스(뼈대·한글 조판) + 에어비앤비(탐색·여행 카드) + 핀터레스트(컬렉션 그리드) +
  우버(칩·플로팅) + 와이즈(환율 표기) 명세 융합 → hallmark 감사 통과(v5) →
  명암비 실측 교정(v5.7)
- 미리보기: claude.ai 아티팩트 "트립디토 디자인 융합안" (홈 4상태 + 보완 화면 6종)

## System

- Genre · playful (soft 계열 — 소비자 여행·쇼핑. 채도 절제, 부드러운 표면, 결코 유치하지 않게)
- 화면 패밀리 · **앱 화면** = 모드 캔버스 시스템 / **스펙·핸드오프 문서** = Long Document
- Theme · custom (vibe: "하늘 · 바다 · 노을 — 복잡함 없이, 여행 쇼핑")
- Axes · light paper / sans(시스템 한글 스택, Pretendard 권장) / blue accent

---

## 1. 모드 시스템 (이 제품의 핵심 규칙)

여행의 `startDate`–`endDate`로 **날짜가 모드를 결정**한다. 사용자 토글 없음.
활성 여행 변경은 **홈 대문의 도시 이름**에서 한다(`도시 ⌄` → 여행 전환 시트).
대문이 이미 "지금 어떤 여행인지"를 말하는 자리이므로 바꾸는 것도 같은 자리에서 한다.

| mode  | 판정                        | 캔버스    | 모드 강조        | 서사        |
| ----- | --------------------------- | --------- | ---------------- | ----------- |
| none  | 활성 여행 없음              | `#FFFFFF` | (기본 블루)      | 구경        |
| idle  | startDate > today + 7일     | `#FFFFFF` | 잉크             | 준비 전     |
| prep  | today + 1–7일 = startDate   | `#EEF5FF` | (기본 블루)      | 아침 하늘   |
| live  | startDate ≤ today ≤ endDate | `#E0F5F1` | `#0D9488` 틸     | 한낮의 바다 |
| after | endDate < today             | `#FFF6E8` | `#A55200` 앰버딥 | 노을        |

**규칙:**

- 모드가 칠하는 층은 **캔버스뿐.** 카드·리스트·버튼·탭바는 모든 모드에서 동일 —
  기능이 숨거나 형태가 바뀌지 않는다
- 모드 캔버스는 **홈에서만** 칠한다. 다른 화면은 칠하지도, 상태 표시 chrome(pill 등)을
  달지도 않는다 — 여행에 귀속되는 액션(담기·리스트 연동)은 **완료 토스트가 대상 여행을
  명시**한다: "도쿄 리스트에 담았어요"
- **홈 하단에 고정 도크를 두지 않는다.** 여행 전환은 대문의 도시 이름이 맡는다 — 화면을
  가리는 고정 레이어도, 그 자리를 비워 두는 여백도 필요 없다. 여행이 하나뿐이면 전환 UI를
  아예 달지 않는다. 카드 전체가 링크인 곳에 버튼을 얹을 때는 **링크를 `absolute inset-0`으로
  깔고 본문을 `pointer-events-none`으로 통과**시킨 뒤 그 버튼만 되살린다 — `<a>` 안에
  `<button>`을 중첩하면 마크업이 깨진다.
  상품 추가는 화면 전체의 행동이므로 **오른쪽 아래 FAB 하나**가 맡는다. 카드 안에 두면
  같은 행동이 화면마다 다른 자리에 생기고, 카드 헤더의 줄 높이까지 버튼 크기에 끌려다닌다.

---

## 2. Tokens (canonical — hex가 원본, `app/globals.css`가 source of truth)

```css
:root {
  /* paper & ink — 쿨 그레이(블루 틴트). 순검정 금지 */
  --color-paper: #ffffff; /* 브랜드 결정으로 순백 유지 */
  --color-paper-2: #f2f4f6; /* 회색 카드·세그먼트 트랙 */
  --color-paper-3: #e9edf2;
  --color-ink: #191f28; /* 본문·제목 — 흰 배경 16.56:1 */
  --color-ink-2: #4e5968; /* 정보성 보조(가격·날짜·개수) — 7.11:1 */
  --color-ink-3: #8b95a1; /* placeholder·비활성·장식 전용 — 3.04:1, 정보 전달 금지 */
  --color-rule: #e5e8eb; /* 1px 헤어라인. 2px 장식 보더 금지 */

  /* semantic — 모드 불변. 화면당 강조 1개 */
  --color-accent: #3182f6; /* 행동: 채움·아이콘·대형(≥24px bold) 전용. 흰 위 3.71:1 */
  --color-accent-text: #1b64da; /* 텍스트 레벨 블루(링크·숫자·개수) — 흰 5.41 · 회색카드 4.91 */
  --color-affect: #ff385c; /* 감정: 찜·좋아요 아이콘 — 구경 탭 한정. 아이콘 3.52:1 */
  --color-success: #03b26c; /* 채움·아이콘 전용 */
  --color-success-text: #037a4b; /* 등락·완료 텍스트, 완료 체크 뱃지 배경 — 5.40:1 */
  --color-danger: #f04452; /* 에러·삭제 — 좋아요와 구분 */
  --color-danger-text: #b42318; /* 에러·초과 금액 텍스트 — 밝은 표면 AA */
  --color-star: #ffb703; /* 별점 전용 앰버 */
  --color-focus: #3182f6;

  /* mode layer — <body data-mode="…">가 이 토큰만 교체. 컴포넌트 코드 불변 */
  --mode-canvas: var(--color-paper);
  --mode-accent: var(--color-accent);
  /* [data-mode="prep"]  --mode-canvas:#eef5ff */
  /* [data-mode="live"]  --mode-canvas:#e0f5f1; --mode-accent:#0d9488;
                         보조: --live-deep:#0f766e(텍스트 4.82) · --live-sub:#446e63(5.06)
                         · --live-tint:#c2e9e1(보더) · --live-ink:#12312b */
  /* [data-mode="after"] --mode-canvas:#fff6e8; --mode-accent:#a55200(텍스트·CTA 5.15/5.52)
                         보조: --after-ink2:#8a6a3a(4.66) · --after-tint:#ffe9c7 */

  /* 선물 태그 3종 — 축약·통합 금지. 태그 글자는 --color-ink */
  --gift-acq: #8ecae6; /* 지인 */
  --gift-col: #ffb703; /* 동료 */
  --gift-fri: #fb8500; /* 친구 */

  /* 여권 전용 팔레트 — 종이·잉크·포일의 물성. 여권 화면 밖 사용 금지.
     명암비는 종이(--passport-paper) 기준 실측. 전체 정의는 globals.css */
  --passport-cover: #102e4e; /* 표지 딥네이비 (+hi/mid/lo/edge 그라디언트) */
  --passport-paper: #f7f8ef; /* 내지 (+hi/mint/warm/nav/load/press) */
  --passport-ink: #183b58; /* 본문 10.87:1 (+ink-2 6.47 · ink-3 4.87) */
  --passport-foil: #d8bd76; /* 표지 포일 — 장식 전용, 종이 위 텍스트 금지 */
  --passport-foil-ink: #836726; /* 종이 위 금색 텍스트 4.99:1 */
  --passport-rule: #638d97; /* 헤어라인 3.39:1 */

  --font-body:
    "Pretendard Variable", Pretendard, -apple-system, "Apple SD Gothic Neo",
    "Noto Sans KR", sans-serif;
  --font-display: var(
    --font-body
  ); /* 별도 디스플레이 서체 없음 — 굵기로 위계 */
  --font-mono:
    ui-monospace, SFMono-Regular, Menlo, monospace; /* 코드·경로 전용 */

  /* radius ladder — 정수 9단. 배수 파생 금지. 사이즈-라운드 페어 */
  --r-xs: 4px;
  --r-s: 8px;
  --r-btn-s: 10px;
  --r-m: 12px;
  --r-l: 14px;
  --r-xl: 16px;
  --r-2xl: 20px;
  --r-3xl: 24px;
  --r-full: 999px;
  /* 버튼 XL→16 L→14 M→12 S→10 · 입력 12 · 카드 16 · 시트 20 · 칩 full */

  /* spacing — 4pt 그리드. outer padding: 도구 20 / 탐색 16 / 컬렉션 그리드 12 */
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 20px;
  --space-6: 24px;
  --space-8: 32px;
  --space-10: 40px;
  /* 상단바 padding 16px, 상단바 아래 본문 시작까지 16px 이상 */
  /* 리스트 행 padding 12px, 회색 카드 12/16px */

  /* elevation — 목적이 다르면 깊이도 다르다 */
  --lift-flat: none; /* 컬렉션 그리드 */
  --lift-card:
    0 0 0 1px rgb(25 31 40 / 0.02), 0 2px 6px rgb(25 31 40 / 0.04),
    0 4px 8px rgb(25 31 40 / 0.08); /* 탐색 카드 (에어비앤비 3중) */
  --lift-float: 0 2px 8px rgb(25 31 40 / 0.16); /* FAB·플로팅 검색 */

  --ease-out: cubic-bezier(0.22, 0.61, 0.36, 1);
  --dur-fast: 120ms;
  --dur-base: 200ms;
  --dur-slow: 320ms;
}
```

## 3. Typography (토스 램프 — 한글 기준)

- 본문 기본 `15px / 1.5 / -0.5% / 400` · 큰 본문 17px · 캡션 `12px / 1.4 / 500`
- 제목 h1 `28 / 1.3 / -2% / 700` · h3 `22` · 카드 타이틀 `18 / 1.45 / 600` · 라벨 `15 / 1.25 / 600`
- 대문 전용 대형: N일차 `52px / 700 / -4.5%` · D-day `38px / 700` · 결산 `27px / 700`
- 헤딩은 항상 정체(roman) — 이탤릭 헤더 금지. 강조는 굵기·강조색으로
- 금액·환율·수량·카운트는 `font-variant-numeric: tabular-nums` — 예외 없음
- 금액 표기 `¥2,400` / `1,000원` (통화 붙여쓰기) · 원화 환산 병기 `≈ 22,300원`
- **최소 크기 9px.** 12px 미만은 여권 각인처럼 질감을 위한 장식 라벨에만 허용하고,
  한글은 10px 아래로 내리지 않는다(자소가 뭉갠다). 라틴 대문자 각인만 9px까지 허용하며
  이때 트래킹은 0.18em 이하로 둔다 — 좁은 화면에서 줄바꿈되지 않게

## 4. 명암비 (WCAG AA 실측 규칙 — 2026-08-11 계산)

- **정보성 텍스트는 4.5:1 이상.** ink-2까지가 정보 전달용 하한 — ink-3는
  placeholder·비활성·장식 전용, 가격·날짜·개수에 쓰지 않는다
- **채도 원색(블루·오렌지·그린)은 텍스트에 직접 쓰지 않는다** — 채움·아이콘·대형
  (≥24px bold) 전용. 텍스트 레벨은 딥 변형(`*-text`, `*-deep`)을 쓴다
- 알려진 예외(브랜드 결정): 흰 글자/블루 CTA 채움 3.71:1 — 토스 프로덕션과 동일 조합,
  볼드 라벨 한정
- 여권 팔레트도 같은 기준을 따른다. **포일색(`--passport-foil`)은 네이비 표지 위에서만
  텍스트로 쓰고**(7.53:1), 종이 위 금색 텍스트는 반드시 `--passport-foil-ink`(4.99:1)를 쓴다

### 색을 인라인으로 쓰지 않는다 — 두 가지 허용 형태

1. **토큰 참조** — 마크업·스타일의 모든 색은 `var(--token)`. JSX className에 hex 직접 기입 금지
2. **타입이 붙은 중앙 데이터 테이블** — 국가별 도장 잉크(`passport-stamp.ts`),
   페이지별 보안 인쇄(`passport-security-paper.ts`)처럼 **디자인 데이터가 값 자체인** 모듈은
   해당 파일에 hex를 두되, 타입을 명시하고 한 파일에 모은다. 이 두 파일 외 신설은 금지

투명도만 다루는 그림자·질감 `rgba()`는 색 토큰이 아니라 `--lift-*`와 같은 범주로 본다.

---

## 5. 구조 (IA)

- 모바일 하단 탭 **5개 아이콘 전용**: 쇼핑 · 때샷 · 홈 · 여권 · 프로필.
  엄지 도달이 쉬운 가운데에는 홈을 두고, 완료한 여행 기록은 독립된 여권 탭에서 바로 연다.
  시각 라벨은 두지 않되 각 링크의 `aria-label`은 반드시 유지한다. 활성 상태는 아이콘 굵기·색·
  표면 표시와 `aria-current`를 함께 사용한다.
- 제품은 모바일 전용이다. 320–480px에서 전체 폭을 쓰고, 더 큰 화면에서도 480px 앱 레일을
  중앙에 유지한다. 하단 5탭과 모바일 단일 열 흐름은 viewport와 무관하게 바뀌지 않는다.
  단, 700px 이상 900px 미만 폴더블의 여권 화면만 한 면에 완전한 물리 페이지 하나씩 총 두 페이지를
  보여 준다. 900px부터는 다시 480px 모바일 레일로 돌아간다. 상단 데스크톱 내비는 만들지 않는다.
- **상단바 한 줄 원칙 — 제목은 대신할 것이 있을 때만 비운다.**
  하단 탭 아이콘이 이미 화면 이름을 말하므로, 그 자리에 더 많은 일을 하는 요소가
  있으면 제목을 없애고 그 요소를 **헤더 높이(h-12) 안으로 올린다.** 제목 줄과 탭 줄을
  따로 쌓지 않는다 — 모바일 전용 앱에서 44px은 본문에 쓰는 게 낫다.
  Apple HIG도 *"제목이 중복으로 보이면 비워도 된다"*고 명시한다.
  - 홈 = 워드마크 / **여행 = 내 여행·여권 탭** / **때샷 = 피드·잇템 랭킹·커뮤니티 탭**
    (+우측 액션) / 프로필 = 신원 카드가 제목 역할 / 서브 화면 = 뒤로가기 + 제목
  - **대신할 것이 없으면 제목을 남긴다** — 쇼핑처럼 하위 탭이 없는 채널은 제목 유지
  - 비운 제목은 반드시 `sr-only` `<h1>`으로 남기고 활성 탭에 `aria-current`를 준다.
    아이콘 단독 버튼은 `aria-label` 필수다.
- **상단 분기 탭은 fixed 규격이다 — 화면 전체를 균등 분할한다.**
  Material 규격상 탭 폭 = 컨테이너 ÷ 탭 개수이며, 작은 화면에서는 전체 폭을 채운다.
  글자 폭에 맞춰 왼쪽으로 몰아 두는 건 **탭이 많거나 가변일 때 쓰는 scrollable 규격**이라
  개수가 고정된 우리 화면에는 틀리다. 위치가 항상 같아야 근육 기억이 생긴다.
  - 높이 48px(h-12) · 활성 밑줄 2px, **탭 폭 전체**(`inset-x-0`) · 글자 15px
  - 활성 = 굵기(bold) + 잉크색 + 밑줄, 비활성 = medium + ink-2
  - 좌우 여백을 주지 않는다 — 탭이 화면 끝까지 닿아야 균등 분할이 성립한다
  - 탭 개수는 4개 이하. 5개부터는 답답해지므로 화면을 나눈다
  - 최소 탭 폭 72px 확인: 320px 화면에서 3탭 = 107px ✅ / 2탭 = 160px ✅
  - 포커스 링은 `ring-inset` — 탭이 화면 끝에 닿아 바깥 링이 잘린다
- 아이콘 글리프는 기능을 말한다: 설정=톱니 · 여행 수정=연필 · 슬라이더=필터 전용.
  아이콘은 단일 스트로크 셋 — 기본 1.8(1.7–2.0 허용), 초소형 체크 글리프만 예외(3.4+).
  이모지 장식 금지
- 홈은 **활성 여행의 쇼핑 컨텍스트**를 보여 주고, 그 여행을 **대문에서 바로 전환**할 수 있다
  (도시 이름 + `⌄` → 여행 전환 시트). 여행 생성과 전체 여행 관리는 여행 탭이 맡는다 —
  홈에 고정 도크나 헤더 생성 버튼은 두지 않는다.
- 지도 = 서브 화면(탭바 없음, 뒤로가기). 진입: 헤더 지도 아이콘 · 여행중 홈 근처 매장 카드
- 광고 2종 유지: 홈 하단 AD 라벨 배너 · 쇼핑거리 진입 다이얼로그("오늘 하루 보지 않기")

## 6. 화면별 스펙

**홈** — 선택한 여행의 **여행 이미지 배너 → 쿠폰 → 접힌 쇼핑 체크리스트 → 환율 → AD 배너**
순서를 고정한다. 여행 이미지 배너는 홈의 첫 콘텐츠이며 삭제하거나 체크리스트 아래로 옮기지 않는다.
여행 목록·상태 설명 문구는 중복 배치하지 않는다.

**홈 체크리스트는 목록이 주인공이다.** 화면에서 가장 크게 읽혀야 하는 건 상품 이름이지 섹션
제목이 아니다. 제목은 **17px**로 두되 카드 타이틀(18px)까지는 올리지 않는다. 목록의
우선순위는 제목을 작게 만들어서가 아니라 행 밀도와 미리보기 줄 수로 지킨다. 줄 높이는 글자에만
맞춘다 — 옆에 큰 버튼을 두면 줄 높이가 그 크기에 끌려가 **패딩을 0으로 해도 위아래 여백이
남는다.** 일차·상태 필터
칩도 보이는 높이를 28px로 줄인다(터치 영역은 `after`로 44px 유지). 리스트 위를 가로지르는
추가 버튼은 카드 안에 두지 않는다. **상품 추가는 화면 오른쪽 아래 FAB**가 맡고(56px, 탭바와
안전 영역 위, 앱 레일 안쪽 정렬), 카드에는 제목만 남는다 — 줄 높이를 버튼에 맞출 필요가 없어져
제목 위아래 여백이 글자 크기만으로 정해진다. 직접 추가와 전체 목록은 여행 상세가 맡는다.
FAB는 `fixed`라 카드의 `overflow-hidden`에 잘리지 않지만, **조상에 `transform`·`filter`가
있으면 그 순간 잘린다** — 앱셸에 그런 속성을 넣지 않는다. FAB가 뜨는 화면은 마지막 카드가
가리지 않도록 `FAB 높이 + 16`만큼 본문 아래 여백을 더한다.

**추가 아이콘은 `배지` 관례를 따른다.** 구글 Material `add_a_photo`와 애플 SF Symbols
`camera.badge.plus`가 같은 규칙을 쓴다 — `+`는 **본체 바깥 오른쪽 위에 붙는 배지**이고, 본체는
배지가 앉을 자리(오른쪽 위 사분면)를 열어 주되 **자기 형태는 그대로 유지**한다. 렌즈를 `+`로
바꾸는 식으로 본체의 식별 요소를 대체하면 무엇에 더하는 건지 알 수 없어지고, `+`를 따로 떼어
나란히 놓으면 버튼이 두 개로 보인다. lucide에 `camera-plus`가 없으면 직접 그리되 자매
아이콘(`image-plus`·`file-plus`)의 치수를 그대로 쓴다 — 배지는 (19, 5) 중심에 팔 길이 3,
본체는 배지와 3 이상 떨어뜨린다. 글자가 없으므로 `aria-label`이 유일한 이름이니 반드시 남긴다.

**추가 버튼은 36px, 제목 줄 높이도 36px(`leading-9`)로 맞춘다.** 줄 높이가 버튼 크기에
끌려다니면 위아래 여백이 제멋대로 생긴다. 버튼을 칩(28px)에 맞춰 줄이면 표준 아이콘
버튼(Material 40dp)보다도 작아져 눌러도 되는 것으로 보이지 않는다. 44px까지 키우면 제목 줄이
그만큼 두꺼워지므로, **보이는 크기 36px + `after`로 채운 터치 영역 44px**이 이 밀도에서의 답이다. 높이는 칩과 같은 28px, 터치 영역은
`after`로 44px. 이렇게 비운 높이는 미리보기를 한 줄 더 보여 주는 데 쓴다(4줄).
**필터는 모드와 무관하게 일차 하나다**(`전체 / 1일차 …`). 여행 중이라고 구매 상태로 바꿔
걸지 않는다 — 상태는 각 행의 체크박스와 제목 옆 숫자가 이미 말하므로, 같은 정보를 거르는 축으로
또 만들면 칩 줄만 길어진다. 칩의 크기·글자·간격·스크롤 동작도 **한 곳에서만 정의**한다.
분기마다 스크롤러 마크업을 따로 쓰면 한쪽에만 드래그 후 클릭 차단이 빠지는 식으로 조용히
갈라진다. 비었을 때의 문구도 모드가 아니라 **아무것도 없음 / 거른 결과 없음**으로만 나눈다.

**목록은 `더보기` 버튼이 아니라 카드 안에서 스크롤한다.** 홈 전체를 늘리면 아래의 환율·광고
카드가 리스트 길이에 밀려난다. 스크롤러 높이는 줄 높이의 **정수배를 피해**(62 × 4.5) 다섯째 줄이
반쯤 잘려 보이게 하고, 스크롤이 끝에 닿을 때마다 8개씩 붙인다. 필터가 바뀌면 첫 페이지로 되돌린다.

**대문을 누르면 어느 상태에서든 여행 상세로 간다.** 여행 중에만 지도로 새면 같은 자리를 눌렀는데
다른 곳이 열려서 매번 확인해야 한다. 지도는 헤더 아이콘이 맡는다.

**여행 상태 배지는 정의가 하나다**(`TripStatusBadge`). 여행 탭 카드와 홈의 여행 전환 시트가
같은 컴포넌트를 쓰므로 `여행 중 / 출발 예정 / 여행 완료`라는 이름과 색이 화면마다 갈라질 수 없다.
`idle`도 사용자에게는 그냥 출발 예정이다.

상품 행은 **금액과 수량을 한 줄로 붙이고(`¥3,600 · 3개`) 일차는 넣지 않는다** — 바로 위에 일차
필터가 있어 지금 무엇을 보고 있는지는 이미 알 수 있고, 두 줄로 나누면 한 화면에 보이는 상품이 준다.

- 칩 스크롤러에 세로 여백을 줄 때는 `-my-*`로 되돌린다. `overflow-x-auto`는 세로도 `auto`가
  되어, 터치 영역이 컨테이너 밖으로 나가면 세로 스크롤이 생긴다

**이름은 상태에 따라 바뀌지 않는다.** 이 목록은 언제나 **`쇼핑리스트`**다 — `챙길 쇼핑 /
구매 체크리스트 / 여행 쇼핑 기록`처럼 모드마다 이름을 바꾸면 같은 자리의 같은 목록을 매번 다시
읽어야 한다. 달라지는 건 이름이 아니라 옆의 숫자(`8` / `3/8 완료`)다. 같은 이유로 **사진 추가
시트의 사진 고르기 단계는 네 흐름이 모두 같은 문구**를 쓴다(`사진으로 추가` · `디토 AI는 여러
장의 사진도 예측 가능해요!` · `앨범` / `카메라` · `이미지를 선택하세요` · `디토 AI 분석 시작`).
흐름별로 진짜 달라지는 건 가격의 의미(`예상 가격` / `결제 금액` / `기록 가격`)와 저장 결과뿐이다.
**`후보`는 쓰지 않는다** — `살 것 후보 담기`, `후보 3개 저장`처럼 제품에 없는 말이 새어 나온다.

**때샷** — 피드·랭킹·커뮤니티를 화면 안의 단일 세그먼트로 제공한다. 인스타그램 문법:
좋아요 = **하트 아이콘만 라우시 채움, 숫자는 잉크**,
"좋아요 1,204개"는 아이콘 줄 아래 별도 행. 스크랩 북마크는 잉크 아웃라인, 저장 시 채움.
**"담기" 버튼은 피드 카드 위**(시트 진입 전) — 플라이휠 마찰 최소화.

**쇼핑** — 검색바가 관문 + 여행지 칩 레일(활성=잉크 반전). 쇼핑몰 카드 =
사진 + 찜 하트 오버레이 + 면세/쿠폰 뱃지 + 별점. 매거진은 list-row로 위계 분리.

**쇼핑리스트** (`/trips/[id]`) — 한 경로가 여행 날짜에 따라 `idle / prep / live / after` 네 상태로
바뀐다. 상태는 장식 색이 아니라 **같은 저장 데이터에서 무엇을 먼저 보여 주는가**를 결정한다.
예정·여행 중 상품 본문은 한 손 스캔에 맞춘 썸네일+정보 단일 열이다. 예정은 구매 전 계획 화면이므로
`전체 / 살거야 / 샀다! / 선물` 분류와 구매 체크박스를 렌더하지 않는다. 얇은 검색 아래의
`최신순 / 이름순 / 일차순 / 가격 높은순 / 가격 낮은순`은 드롭다운이나 한 덩어리 segmented control이
아니라, 각각 독립 테두리와 44px 조작 영역을 가진 버튼으로 수평 스크롤한다. 예정 행을 왼쪽으로 밀면
오른쪽의 72px 삭제 action이 드러나고, action을 다시 눌러야 삭제한다. 세로 스크롤을 가로 제스처보다
우선하며, 키보드는 `ArrowLeft`·`Delete`로 열고 `Escape`로 닫는다. 구매 상태 분류는 실제 체크가 중요한
live/after에서만 노출한다. 구매완료는 초록 체크, 행 표면·사진·부가정보 암전, 상품명을 가로지르는
좌→우 취소선 모션으로 표현하되 실제 control을 disabled로 만들지 않아 해제·수정이 가능해야 한다.
우상단 연필=여행 수정. 하단: 직접 추가 / 사진으로 추가.
상태 배지는 요약 카드 안에 중복하지 않고 앱 헤더의 여행 제목 바로 오른쪽에 둔다. 제목과 배지는
서로 다른 접근성 노드이며, 320px에서는 제목만 말줄임하고 `여행 예정 / 여행 중 / 여행 완료` 배지와
양쪽 44px 헤더 버튼은 그대로 유지한다. 모든 상태 요약은 **큰 중앙 여행가방 stage · 바로 아래 2열
status rail**의 같은 `central-instrument task console` 골격을 공유한다. 가방 외곽과 중심 좌표는 상태가
바뀌어도 움직이지 않고, 내부 장면만 예정=`packing`, 여행 중=`transit`, 완료=`unpacking`으로 바꾼다.
가방은 클레이·AI 래스터 이미지를 쓰지 않는다. 기존 평면 line-art를 기준으로 앞면·상단·오른쪽 면을
하나의 2px 외곽선과 연결 원근선으로 잇고, 뚜껑·손잡이가 본체 앞뒤로 겹치는 순서를 명확히 한다.
입체감은 새 장식이나 과한 광택이 아니라 작은 side-face offset, 이중 외곽선, 상태색 안의 절제된 명암
면으로만 만든다. 예정은 열린 뚜껑 위의 물건이 가방 안으로 정착하는 `packing`, 여행 중은 닫힌
`transit`과 구매 진행 fill, 완료는 열린 가방 주변에 기록 물건이 펼쳐진 `unpacking`이다. 카드 전체는
나뉜 metric box 세 개가 아니라 하나의 연속 surface이며, stage 다음 status rail에서 가장 중요한 값을
크게, 보조 값을 작게 읽는다. 장면 전환은 transform·opacity만 쓰고 reduced-motion에서는 최종 장면을
즉시 보여 준다.


- **idle / prep · 예산을 정하고 담는 단계** — 중앙 packing stage 아래 status rail에서 모든 상품의
  `예상가×수량` 합계인 **총 예상 비용**, 입력 중인 **예산**, `giftTags`가 하나 이상인 **담은 선물 수**를
  한 묶음으로 읽게 한다. 별도 저장 체크 버튼은 두지 않는다. 유효 입력은 gauge에 즉시 반영하고 450ms
  뒤 자동 저장하며 blur에서는 대기 중 값을 즉시 flush한다. 입력을 지우면 `아직 모름`, `0`은 명시적인
  0원으로 구분하고 저장 중·오류·완료는 같은 helper slot에서 접근 가능하게 알린다. `예산 충족률`은
  카드 왼쪽 edge의 높이를 대부분 채우는 대형 세로 reservoir/HP로 두고 총 예상 비용과 시각적으로
  연결한다. HP는 가방과 같은 2px stroke, bevel corner, 앞면·오른쪽 side face, 상태 palette를 써서
  하나의 기기처럼 보이게 한다. 물은 bottom-origin `scaleY`로 차고 수면만 transform·opacity로 움직인다.
  `설정 예산 ÷ 총 예상 비용`을 정확히 보여 주며, 예산이 예상 비용보다 적으면 그 비율만큼 채우고
  같거나 많으면 100%에서 멈춘다. 예산 미입력은 빈 상태이며, 예상 비용이 0이면 가짜 비율을 만들지
  않고 현재 예상 비용이 모두 충족된 상태로 표시한다. 비율이 있을 때는 progressbar로 읽고
  예산·예상 비용·충족률·부족액을 함께 알린다. 옆 여행가방에는 저장한 상품마다
  셔츠·카메라·안경·지도·헤드폰 아이콘을 하나씩 최대 5개까지 켜고, 그보다 많으면 정확한 `+N`을
  표시한다. 이 가방도 진행률이 아니라 상품 컬렉션 status다.
- **live · 남은 돈과 구매 성취를 함께 보는 단계** — 금액 영역의 첫 정보는 **남은 예산**이며
  `(예산 - 기록 금액) ÷ 예산`을 별도의 잔액 gauge로 보여 준다. 구매 체크로 잔액 gauge가 줄어드는
  동시에 여행가방은 `구매 완료 상품 수 ÷ 전체 상품 수`만큼 아래에서 위로 채워져, 비용 정보와 긍정적인
  완료 피드백의 역할을 섞지 않는다. 예산 미입력·0은 잔액 비율을 만들지 않고, 초과 시 잔액은 0에서
  멈추며 정확한 초과액을 표시한다.
- **after · 여행 쇼핑 결산 단계** — 상단은 기존 주황색 결산 표면과 공통 중앙 여행가방을 유지한다.
  가방은 live와 같은 `구매 완료 상품 수 ÷ 전체 상품 수`이며 필터 결과가 아닌 저장한 전체 상품을
  분모로 사용한다. 가방 안의 옷·카메라·안경·지도·헤드폰은 선이나 칸으로 나누지 않은 하나의 여행 물건
  콜라주이고, 하나의 전역 마스크가 가방 표면과 물건을 함께 드러낸다. 가시 요약은 **구매 기록 N개**와
  구매완료 상품의 `예상가×수량` 합계인 **기록 금액** 두 개뿐이다. after에는 예산·남음·초과 3행과
  금액 사용률 gauge를 다시 넣지 않는다. 상품이 없을 때는 가짜 완료율을 만들지 않으며 남은 개수 압박,
  포인트·레벨 같은 과소비 유도형 점수는 만들지 않는다.

모든 상태의 금액은 긴 통화 문자열을 길이 tier로 축소해 320px에서도 한 줄과 기준선 정렬을 보존한다.
가격 확인이 필요한 기록 금액은 설명 문단을 추가하지 않고 라벨의 경고 아이콘과 접근성 이름으로만
알린다. 금액 gauge와 가방은 서로 다른 접근성 이름을 사용하고, 분모가 없는 상태는 progressbar가 아닌
status로 읽는다. 체크 시 가방의 단일 마스크와 잔액만 200ms 이내로 갱신하고 reduced-motion에서는
즉시 반영한다.

여행 상세의 헤더·상태 요약·여행가방·분류/검색/정렬 도구와 하단 추가 액션은 viewport 안에 고정한다.
100% 미만의 예정 예산은 소수 첫째 자리에서 내림해 표시하며, 예를 들어 14,090÷14,160은 99.5%다.
화면 값·게이지·`aria-valuenow`·접근성 설명이 같은 값을 사용하고, 정확히 같거나 많은 경우에만 100%를
말한다.

실제로 스크롤되는 곳은 상품 목록 section 하나뿐이며, 상태 stage·얇은 검색·분류/정렬 rail은 고정한다.
로딩·빈 상태·일반 단일 열·완료 3열 grid가 모두 같은 내부 스크롤 영역을 사용한다. 검색·분류·정렬을
바꾸면 목록만 맨 위로 돌아가고 상단 gauge의 위치는 움직이지 않는다. 320px의 짧은 화면에서도 상품
영역은 최소 44px 이상 남고 마지막 상품은 하단 액션에 가려지지 않아야 한다. 가능하면 88px 상품 행
하나가 첫 화면에 완전히 보여야 하며, 중앙 stage와 status rail은 `max-height: 640px`에서 함께 압축한다.

live/after에서 마지막 미구매 상품의 저장이 성공해 같은 목록이 미완료에서 100%로 전환될 때마다
여행가방이 짧게 한 번 뛰고 결정적인 색종이 9개가 주변으로 흩어진다. 체크를 해제한 뒤 다시 전체 완료하면
새로운 단조 증가 nonce로 다시 재생한다. 초기 100% 로드·필터 변경·쿼리 재검증·실패 후 rollback·상품
삭제로 인해 100%가 된 경우에는 재생하지 않는다. 예정·여행 중 상품 행은 88px 썸네일, 10px 행간,
한 줄 상품명과 압축 메타를 기본으로 하며 체크의 시각 크기와 별개로 44px 조작 영역을 보장한다.
모션은 가방의 시각 레이어와 색종이의 transform·opacity만 사용하고, 음향·진동·무작위 값은 쓰지 않는다.
`prefers-reduced-motion`에서는 팡파레를 숨기고 100% 상태와 `모두 구매 완료` 접근성 문구만 즉시 갱신한다.

after의 하단 고정 영역은 **`도장 찍으러 가기` primary CTA + 48px `+` 아이콘 버튼** 한 행이다.
`+`는 action sheet를 열어 `직접 추가 / 사진으로 추가` 두 48px 행을 제공한다. 직접 추가는 결산
구매 기록 intent를, 사진 추가는 종료일을 구매일로 쓰는 결산 분석 intent를 유지한다. action sheet를
닫은 뒤 사진 시트를 열어 두 focus trap이 겹치지 않게 하고, 닫을 때는 `+`로 포커스를 돌려보낸다.
예정·여행 중의 기존 직접 추가 / 사진 추가 두 버튼은 유지한다.

`도장 찍으러 가기`는
`/passport?view=stamps&stampTripId=<encoded-id>&returnTo=<encoded-trip-detail>`로 이동한다. 완료 여행만
대상이 될 수 있고, 처음 진입한 대상 도장은 자동으로 나타나지 않는다. 휴대폰은 완전한 물리 페이지 한 장,
폴더블은 펼쳐진 두 장 각각을 44px 이상의 페이지 버튼으로 선택하게 한다. 선택하면 기존 국가별 도장을
그 페이지의 겹치지 않는 칸에 찍고, `tripId → pageNumber`를 계정 범위 로컬 메타데이터에 저장한 뒤 URL에
`stampPage=<n>`을 더한다. 인영은 잉크가 눌리는 scale·opacity·rotation으로 나타나며 reduced-motion은
즉시 완료한다. 이미 저장된 도장은 새로 만들지 않고 실제 페이지를 열며, 잘못된 ID·미완료 여행은 일반
여권을 깨뜨리지 않고 안전한 상태 안내를 보여 준다. 도장을 눌러 여행 상세로 들어갔다가 돌아올 때는
`stampTripId`·`stampPage`와 바깥 `returnTo`까지 중첩 복귀 경로에 그대로 보존한다.

상품 영역은 `추가한 상품들 / N개` 같은 시각 제목·개수 행 없이 접근성 이름이 있는 section에서
바로 시작한다. 모든 상품은 **1px 간격·각진 모서리·그림자 없는 균일 3열 정사각 grid**로 배치하며
320px·390px에서 가로 overflow가 없어야 한다. 사진이 없으면 다른 상품 사진을 대신 넣지 않고
상품명 기반의 결정적 DITO 패턴을 사용한다. 타일 상세 버튼과 형제 관계인 44px 구매 체크를 이미지
좌상단에 두어 즉시 토글하되 실제 체크 표면은 작게 유지한다. 선물 대상 태그도 간결히 노출한다.

상세 버튼의 keyboard focus와 fine-pointer hover에서는 이미지를 어둡게 하고 상품명·기록 금액·
구매처 메모·구매 일차·수량을 web glass 미리보기로 보여 준다. glass는 읽히는 solid dark를 기본값으로
두고 `backdrop-filter` 지원 시에만 투명도와 blur를 더하며 reduced-transparency에서는 solid로
돌아간다. touch tap은 모바일 bottom sheet를 열고 구매 토글·찜·수정의 44px 액션과 복귀 경로를
유지한다. 검색·정렬·필터(`살거야`/`샀다!`)·직접 추가·사진 추가 기능도 사라지면 안 된다.

**사진으로 추가** (시트) — 분석 결과 rows + **수량 스피너**(`− 값 +` 캡슐), CTA가 개수·
합계를 직접 말함: "리스트에 추가 · 8개 ¥8,152".

**피드 업로드** (`/shots/new`) — 위계: 여행 선택 → 사진(≤10) → 핀 → 리스트 연결 → 본문.
포커스 입력 = 흰 배경 + 1.5px 블루 보더. 하단 고정 CTA "업로드".

**프로필** — 상단바 완전히 비움. 순서:

1. **신원 카드**(에어비앤비) — 아바타+이름+요약, 수정 버튼 내장
2. **보드형 컬렉션**(핀터레스트) — 내 때샷·스크랩, 커버 이미지+라벨+개수
3. **유틸리티 행**(토스) — 좋아요 → 쿠폰 → 데이터 관리(저장 공간·데모·초기화) →
   **설정(계정·알림·로그아웃)**. 설정은 목록 맨 아래 행, 상단 아이콘으로 빼지 않는다

**여권** — 여행 관리 허브인 독립 1차 탭이다. 기본 화면은 진행 중·예정·완료 여행 목록이며,
각 여행 카드에는 아이콘과 문구를 함께 사용한 상태 배지를 항상 표시한다. 상태 배지는 색만으로
구분하지 않으며 320px에서도 여행명과 충돌하지 않는다. 상단의 단일 세그먼트에서 `내 여행 / 여권`을
전환한다. 기존 `/my-trips` 진입은 이 기본 화면으로
연결한다. 여권 화면은 모달을 사용하지 않고 휴대폰에서 완전한 물리 페이지 한 장을 보여 준다.
700px 이상 900px 미만 폴더블은 완전한 페이지
두 장을 한 면씩 배치한다. 도장은 국가별 SVG 인영·마모·고스트 잉크를 사용하며, 도장을 눌렀을 때만
해당 여행 기록으로 이동한다.

**도장 크기는 px이 아니라 칸으로 정한다.** px을 박아 두면 320px 화면에서는 넘치고 430px 화면에서는
종이가 절반쯤 빈다 — 실제 내지는 270×295부터 364×566까지 가로세로비가 완전히 다르기 때문이다.
칸 안에서 실제 폭은 `min(100cqw / widthFactor, 100cqh / heightFactor)`로 푼다. 두 제수는
**기울어진 뒤의 외곽**이 칸을 넘지 않는 최대치이며, 반드시 **올림**한다 — 내리면 도장이
서브픽셀만큼 칸을 넘는다.

**배치는 4단 계단이다 — 정사각 격자가 아니다.** 정사각 칸(145×218)에 정사각 도장(145×145)을
넣으면 칸마다 세로 73px이 버려지고 그게 두 줄이라 화면 한가운데가 통째로 남는다. 잉크가 종이의
43%밖에 덮지 못했다. 지금은 칸이 **55%×39%**이고 좌우를 번갈아 밟는 네 단이 종이 세로를 처음부터
끝까지 훑는다(단 간격 = `(100 − 39) / 3`). 좌우 두 열이 10% 물리므로 도장이 커진다 —
412×915에서 149px → 181px(+21%).

**각도·겹침은 실제 출입국 도장 규정을 따른다.** EU `Practical Handbook for Border Guards`
(Schengen Borders Code 부속서 IV의 실무 지침)는 도장을 **가로 방향으로 찍어 읽기 쉽게** 하고,
**이미 찍힌 도장 위에 겹쳐 찍지 말며**(타국 도장 포함), 가능하면 **시간 순서대로** 찍고 출국 도장은
입국 도장 가까이 두라고 권고한다. 그래서:

- **도장은 거의 똑바로 선다 — 전체 −6°~+5°.** 각도를 키우면 채움에는 유리하지만 여권이 아니라
  **스티커 콜라주**가 된다. 자리 기울기 ±2°, 여행별 흔들림 ±1°, 나라별 기본 각도 ±3° 안쪽으로
  묶는다. "손으로 눌러 살짝 어긋난 것"까지가 한계다
- **겹침은 이웃한 단의 모서리만, 잉크 기준 14%까지.** 두 단 이상 떨어진 도장은 **절대** 겹치지
  않는다(실측 0%) — 포개지면 기록을 못 읽는다
- 채우는 순서는 **위에서 아래로**, 곧 시간 순서다

**규칙성이 보이면 실패다.** 등간격(20/20/20)에 좌우를 두 값으로만 두면 "좌우 번갈아, 등간격"이라는
규칙이 그대로 읽힌다. 다만 **불규칙은 각도가 아니라 간격으로 만든다** — 각도를 흩는 건 위 규정과
정면으로 어긋난다. 자리표(`CELL_PATTERNS`)에서:

- **세로 간격**을 서로 다르게(예: 19/25/17, 17/28/16). 마지막 단은 `top 61`로 고정해 네 단이
  종이 세로를 정확히 채우고, `top[i] + 높이 + 여백 ≤ top[i+2]`로 먼 쌍의 무겹침을 보장한다
- **좌우**를 같은 쪽 안에서도 어긋나게(왼쪽 0–3, 오른쪽 42–45) — 두 값만 쓰면 열로 읽힌다

흔들림은 여행 ID와 페이지에서 결정적으로 뽑으며 무작위 값으로 수화 결과가 바뀌면 안 된다. 손맛을
내는 크기 변주는 **줄이는 방향으로만** 준다(0.94–1.0) — 1을 넘기면 회전 여유로 잡아 둔 칸을
넘어선다. 각도를 줄이면 회전 여유가 줄어 **같은 칸에서 도장이 오히려 커지므로**, 칸을 62%→55%로
좁혀 겹침을 30%→14%로 낮추고도 크기는 거의 지켰다(작은 화면 +2%, 큰 화면 −8%).
내지 여백은 도장 자리를 뺏지 않을 만큼만 남기고(좌우 8–12px, 위 12px), 아래는 넘김 버튼에 가리지
않을 최소치(56px)만 둔다.
표지는 한국 신여권을 연상시키는 딥 네이비와 절제된 골드 포일을 쓰되 공식 문장이나 정부 문서를 복제하지
않고 `DITO PASSPORT`임을 명확히 한다. **표지 각인은 문장 + 이름 한 덩어리로 가운데 정렬한다** —
설명 문구(`여행의 기억` 같은 부제)도, 반대쪽 끝의 브랜드 배지도 붙이지 않는다. 실물 여권 표지에 이름이
두 번 적히는 경우는 없고, 표지는 여권 본문이 아니라 여권이 무엇인지만 말하는 자리다. 이름은 표지에서
가장 큰 글자(17–19px, 트래킹 0.12–0.14em)로 두되 줄 높이는 36px에 고정해 여권 본문 높이를 뺏지
않는다. 문장은 **여권 문장의 형식(원형 메달리온 + 다섯 꽃잎)만 빌리고 가운데는 나침반**으로 바꾼 디토
고유 마크다 — 대한민국 나라문장은 정부기관 전용이므로 태극과 국장은 어떤 형태로도 쓰지 않는다. 내지는 최근 한국 여권 사증면의 저채도 보안 인쇄에서
`민트·하늘·살구색의 큰 곡선`, `미세 파형선`, `페이지별 비대칭 변주`라는 시각 문법만 참고한다. 태극·국장·
실제 문화재·`VISAS`·국가 식별 마크는 사용하지 않고, 산 능선·해안 파동·수하물 태그·경유지를 추상화한
DITO 고유 여행 기억 모티프로 구성한다. 각 물리 페이지는 단독으로 완결되어야 하며 폴더블에서도 두 페이지를
이어 붙인 한 장의 그림처럼 만들지 않는다. 도장 뒤 장식은 낮은 대비를 유지하고, 작은 화면에서도 국가별
세부와 날짜가 배경보다 먼저 읽히는 크기와 농도를 우선한다.

**지도** — 플로팅 검색 필 + 마커 + 내위치 원형 FAB. 장소 시트 = 쿠폰 뱃지 + 별점 +
도보 시간 + [길찾기 | 리스트에 담기].

**로그인** — 소셜 우선(카카오 #FEE500+검정 라벨, 네이버) → 구분선 → 이메일. 버튼 카피는
행동을 직접: "카카오로 3초 만에 시작".

## 7. CTA voice

- Primary · `--color-accent` 채움 · 라운드 사이즈 페어 · **버튼은 일어날 일을 말한다**
  ("리스트에 추가 · 8개 ¥8,152"). "여기를 눌러주세요" 금지
- Secondary · `--color-paper-2` 채움 + ink · 같은 라운드
- 하단 고정 CTA 위 `white → transparent` 보호 그라디언트 (허용된 유일한 그라디언트)
- 카피는 해요체. 격식체·단정형 `-다`·마케팅 과장 금지. 에러는 다음 행동을 안내

## 8. Motion stance

- motion-cut에 가까움: 로드 오케스트라 없음, 스크롤 페이드 없음, 스켈레톤 시머 금지
- 허용: 체크 토글·시트 진입(`--dur-slow`)·여권 물리 페이지 전환·
  pressed 오버레이(검정 26%). 바운스/오버슈트 금지
- Reduced-motion fallback · ≤150ms opacity crossfade

### 여권 넘김 — 유일하게 손끝을 따라가는 모션

모바일 전용 제품이므로 **버튼·키보드·드래그 세 경로 모두**로 넘어가야 하며 셋은 같은
결과를 낸다. 드래그는 포인터 이벤트 하나로 터치와 마우스를 함께 처리한다.

- **축 판정 먼저** — 10px 이동 전에는 아무 것도 하지 않고, 가로가 우세할 때만
  포인터를 캡처한다. `touch-action: pan-y`로 세로 스크롤을 빼앗지 않는다
- **책 넘김이지 슬라이드가 아니다.** 여권은 **왼쪽이 제본선**이다. 종이는 그 축을
  중심으로 `rotateY` 회전만 하고, **`translateX`로 옆으로 밀지 않는다** — 밀면 종이가
  책 밖으로 빠져나와 넘김으로 읽히지 않는다
- **3층 구조를 지킨다** — 3D 뒤집기의 표준(scene → card → faces)이다.
  `.passport-book`(scene)이 `perspective: 900px`, `.passport-leaf`(card)가
  `transform-style: preserve-3d` + 회전, `.passport-face`(앞/뒤 면)가
  `backface-visibility: hidden`을 맡는다
### 여권은 닫힌 표지로 시작한다

들어오자마자 펼쳐진 내지를 보여 주지 않는다. **표지를 먼저 보여 주고 사용자가 열어야** 안이
나온다 — 여권을 여는 행위 자체가 이 화면의 서사다.

- **표지는 쪽 넘김과 완전히 같은 장치를 쓴다.** 같은 제본선(왼쪽), 같은 90° 범위,
  같은 `--flip-p` 곡면 음영, 같은 캐스트 섀도, 같은 rAF 구동과 지속 시간(`FULL_TURN_MS`).
  표지만 CSS `transition`으로 따로 만들면 곡률도 그림자도 없이 판때기가 도는 것처럼 보여,
  바로 다음에 이어지는 쪽 넘김과 어긋난다 — 같은 동작이면 같은 코드에서 나와야 한다
- **표지도 앞뒤 두 면을 갖는다.** 한 면만 두면 젖혀지는 내내 두께 없는 종이 쪼가리처럼
  보인다 — 쪽 넘김에서 이미 겪고 고친 문제이므로 표지에서 되풀이하지 않는다. 뒷면은
  도장 없는 면지다
- **표지는 직접 눌러 연다.** 대신 `눌러서 펼치기` 라벨이 투명도만 숨쉬듯 오간다(2s 반복).
  여권 화면에서 유일하게 허용하는 반복 모션이다 — 표지가 화면을 통째로 덮고 있어 기다리기만
  하면 아무 일도 일어나지 않으므로, 눌러야 한다는 걸 알릴 다른 수단이 없다. 크기·위치는
  건드리지 않는다
- **도장 찍으러 들어온 흐름만 저절로 진행한다.** 표지가 520ms 뒤 열리고, 목표 장까지
  **한 장씩** 넘어간다(장 사이 140ms). 한 번에 건너뛰면 도장이 어디쯤 찍히는지 알 수 없다 —
  넘어가는 장 수가 곧 "여권이 이만큼 찼다"는 정보다. 목표는 이미 찍힌 도장이면 그 장,
  아직이면 **맨 뒤의 빈 장**이다
- 닫혀 있는 동안 내지는 `inert` + `aria-hidden`이고 넘김 버튼도 숨긴다. 보이지 않는 것에
  포커스가 가면 안 된다
- **도장을 찍으러 들어온 흐름(`?stamp=`)은 표지를 건너뛴다.** 목적이 이미 분명한데 관문을
  하나 더 두는 셈이 된다
- reduced-motion은 여는 동작 없이 곧바로 펼친다

- **3D 문맥은 넘기는 동안에만 만든다(`[data-turning]`).** 이건 성능이 아니라 **선명도**
  문제다. `perspective` + `preserve-3d` + `backface-visibility`를 항상 켜 두면 내지가 늘
  합성 레이어로 승격되고, 모바일 브라우저는 그런 레이어를 기기 픽셀(DPR 3)이 아니라 더 낮은
  배율로 래스터한 뒤 확대한다. 도장의 SVG 필터(`feTurbulence`·`feDisplacementMap`)가 그
  확대를 가장 먼저 드러내서 **잉크 가장자리가 깨져 보인다.** 데스크톱은 래스터 배율이
  넉넉해 같은 코드로도 선명하므로 **모바일에서만 티가 난다** — 데스크톱만 보고 판단하면 안 된다.
  같은 이유로 `will-change: transform`도 넘기는 동안에만 건다.
- **`backface-visibility`는 면에만 건다.** 회전체에 걸면 90°를 넘는 순간 장 전체가
  사라져 **아직 다 넘기지도 않았는데 아래 장이 튀어나온다**
- **종이에는 뒷면이 있다.** 앞면은 지금 장, 뒷면(`rotateY(180deg)`)은 도장 없는 내지다.
  뒷면이 없으면 종이가 얇은 면 한 장처럼 보이고 넘김 중간에 비쳐 보인다
- **`perspective-origin`은 기본값(중앙)** — 왼쪽으로 옮기면 종이가 비틀리며 위로 들리는
  것처럼 보인다
- 젖혀지는 장 아래에는 목적지 페이지를 미리 깔아 둔다.
- **회전 범위는 90°까지다 — 180°가 아니다.** 여권을 한 면만 보여 주므로 제본선이 곧
  화면 왼쪽 끝이고, 90°를 넘기면 종이가 책 밖으로 나가 잘린다. 180°로 돌리면
  **애니메이션의 절반이 보이지 않아** 특히 "다음 장"이 어색해진다(모서리까지 세워진 뒤
  남은 시간 동안 아무 일도 없는 것처럼 보인다).
  - 앞으로: 지금 장이 `0° → −90°`로 **제본선 쪽으로 접혀 사라진다**
  - 뒤로: 이전 장이 `−90° → 0°`로 **제본선에서 펼쳐져 나온다**
  - 두 방향이 정확한 역재생이며, 전 구간이 화면 안에서 일어난다
- 회전 범위를 바꾸면 **지속 시간도 함께 조정한다.** 180°→90°로 줄이면서 560ms를 그대로
  두면 절반 속도로 느껴진다(보이는 구간이 같은데 시간은 두 배). 현재 340ms
- **곡면은 기하가 아니라 음영으로 만든다.** 종이를 실제로 휘게 하려면 세그먼트 분할이나
  WebGL이 필요하지만, 상용 라이브러리(StPageFlip의 soft 페이지)는 **2겹 그라디언트**로
  같은 인상을 낸다. 그대로 채택한다:
  1. **내부 음영**(`.passport-leaf::after`) — 제본선 쪽이 어둡고, 중간의 밝은 마루가
     접힘에 따라 44%→68%로 이동한다. 원통에 빛이 훑고 지나가는 표현
  2. **드리운 그림자**(`.passport-cast-shadow`) — 젖혀진 장이 아래 장에 남기는 그림자,
     제본선에서 가장 짙다
- **음영 세기는 `sin(진행률 × π)`** — 종이는 완전히 펴졌을 때(0·1)가 아니라 반쯤 들렸을
  때 가장 굽는다. 선형으로 넣으면 도착 순간 음영이 남아 종이가 더러워 보인다
- `--flip-p`는 `@property`로 `<number>` 등록한다. 등록하지 않으면 keyframe에서 보간되지
  않아 버튼 넘김에 곡면이 생기지 않는다
- **넘김 판정** — 진행률 34% 초과 또는 속도 0.45px/ms 이상의 튕김
- **고무줄** — 넘길 장이 없는 방향은 진행률의 16%만 따라가고 되돌아온다
- **복귀 스프링** — 임계 미달 시 280ms `cubic-bezier(.22,1.2,.36,1)`. 이 값만 유일하게
  오버슈트를 허용한다(종이가 제자리를 찾는 물리 표현)
- **드래그로 움직였으면 도장 링크의 클릭을 삼킨다** — 넘기려다 여행이 열리면 안 된다
- Reduced-motion: 손끝 추적과 스프링을 끄고 넘김만 즉시 수행한다

## 9. What pages MUST share

로고 · 탭바 · 시맨틱 색 의미(블루=행동, 라우시=감정, 그린=완료) · 타입 램프 ·
라운드 ladder · CTA voice · 선물 태그 3색 · 명암비 규칙(§4) · 상단바 규칙(§5)

## 10. What pages MAY differ on

모드 캔버스(홈만) · 그리드 밀도(표면별 outer
padding) · 깊이(탐색=lift-card, 컬렉션=flat)

## Exports

`app/globals.css`가 source of truth. Tailwind v4 `@theme` 매핑은 기존 `--color-*`
변수명 유지(shadcn 호환: `--primary`=accent, `--border`=rule 등).

## 감사 이력

- v5 (2026-08-11) · hallmark 감사 — 가짜 폰 베젤 제거, 토큰 잠금(인라인 hex 0),
  직선 따옴표 교정. 의도적 예외: 순백 paper, 탭 라벨 없음
- v5.4 · 상태 pill 제거(토스트로 대체), 설정=톱니/수정=연필 글리프 교정
- v5.7 · 명암비 실측 — 22개 조합 중 7건 AA 미달 발견·교정(§4 규칙화), 4pt 간격 정규화
- v5.9 · 모바일 IA 정리 — 여행 관리는 홈으로 통합하고 라벨이 있는 4개 1차 내비 유지
- v5.9 · 모바일 전용 규칙 고정 — 480px 앱 레일, 단일 열, 전 viewport 하단 탭 유지
- v6.0 · 엄지 중심 IA — 쇼핑/때샷/홈/여권/프로필 5탭, 홈 여행 도크, 전용 여권 화면과
  폴더블 2쪽 보기, 홈 정보 순서를 배너→쿠폰→체크리스트로 재정렬
- v6.1 · 아이콘 전용 5탭, 헤더 새 여행 제거, 상태별 여행 도크+분리된 생성 액션,
  한국 신여권 모티프와 확대된 국가별 도장 적용
- v6.2 · 최근 한국 여권 사증면의 저채도 보안 인쇄 문법을 DITO route-orbit로 재해석하고,
  페이지별 독립 모티프·결정적 SVG ID·도장 우선 대비를 규칙으로 고정
- v6.3 · 홈 여행 도크를 상태별 고유 물성으로 분리하고, 출발 준비 탑승권의 큰 D-day·공항 경로·
  아이콘 전용 여행 생성을 고정. 완료 여행은 정직한 기록 금액 그래프와 모바일 탭 가능한 상품
  벤토를 사용하는 결산 화면으로 분리
- v6.4 · taste-skill 재감사 - 여행 도크의 작은 상태 라벨·숙박일수·추론 공항 정보를 제거하고,
  준비 전=출발일, 계획=큰 D-day, 여행 중=구매완료 수, 결산=기록 수·기록 금액의 실제 데이터
  위계를 고정
- v6.5 · 홈을 쇼핑리스트 작업 공간으로 축소하고, 여권 탭을 `내 여행 / 여권` 허브로 재구성.
  여행 도크는 상태별 장식 카드를 폐기하고 하나의 타입·간격·높이 시스템으로 통일
- v6.6 · 운영 정보 위계를 다시 잠금. 홈은 이미지 배너 → 쿠폰 → 쇼핑리스트 순서를 유지하고,
  여권의 내 여행 목록은 모든 카드에 아이콘·문구 기반 상태 배지를 표시
- v6.7 · 완료 여행 결산을 시각 제목 없는 균일 3열 정사각 grid와 모바일 bottom sheet로 통일하고,
  기록 금액과 상품 기록을 같은 화면에서 확인하는 흐름을 고정
- v6.8 · 결산 상품 영역을 1px 3열 grid, 독립 44px 체크, 조건부 web glass 상세로 정리
- v6.9 · 기존 주황 결산 표면을 복원하고 여행가방 게이지의 시각 방향을 탐색
- v6.10 · 개별 스티커 칸을 폐기하고, 여행 물건 콜라주와 가방 표면을 하나의 전역 마스크로 묶음
- v6.11 · 결산 상단의 시각 제목·설명 문장을 제거하고 중앙 가방을 확대하면서 내부 구획선을 없앰.
  첫 화면부터 상품 grid가 보이는 높이를 고정
- v6.12 · 캐리어 채움의 의미를 금액 사용률에서 전체 상품의 구매 완료율로 교정. 모든 상품을 체크하면
  예산 잔액과 무관하게 캐리어가 정확히 100% 채워지는 규칙을 고정
- v6.13 · 여권 표지 각인을 `문장 + DITO PASSPORT` 한 덩어리로 가운데 정렬. 부제(`여행의 기억`)와
  오른쪽 끝 `DITO` 배지를 삭제하고, 표지 문장은 여권 문장의 형식만 빌린 나침반 메달리온으로 신설.
  더 쓰이지 않는 `--passport-foil-2 / -dim / -rule` 토큰 제거
- v6.14 · 여행 상세를 idle/prep/live/after 정보 위계로 분리. 예정 예산 즉시 편집과 설정 완료 gauge,
  저장 상품 아이콘 5개+정확한 `+N`, 여행 중 잔액 gauge+구매 가방, 완료 가방+기록 2개를 고정하고,
  완료 하단을 `도장 찍으러 가기`+단일 `+`로 통합
- v6.15 · 여권 한 장의 도장을 2개 → **4개**로 늘리고, 도장 크기를 고정 px에서 2×2 칸 기반
  유동 크기로 교체. 가상 256×320 캔버스와 충돌 검사 엔진을 제거하고 칸 구조로 겹침을 원천 차단
- v6.16 · 2×2 정사각 격자를 **4단 대각 계단**(칸 62%×39%, 좌우 번갈아)으로 교체. 세로로 긴 화면에서
  버려지던 칸 안 세로 여백을 없애 잉크 면적 43% → 79%, 도장 +35%. 이웃 단만 최대 20% 겹침 허용
- v6.17 · 여행 예정의 예산 gauge를 설정 여부가 아닌 `설정 예산 ÷ 총 예상 비용` 충족률로 교정.
  부족하면 실제 비율만큼 채우고 같거나 많으면 100%에서 멈추며, 300ms transform 전환·reduced-motion
  즉시 전환·저장 후 키보드 초점 복귀까지 고정
- v6.18 · 여권 3D 문맥(`perspective`·`preserve-3d`·`backface-visibility`·`will-change`)을
  넘기는 동안에만 켜도록 `[data-turning]`으로 게이팅. 상시 레이어 승격 때문에 모바일에서만
  도장 잉크가 깨져 보이던 문제 수정
- v6.19 · 여행 상세의 상태 요약·여행가방·분류/검색/정렬과 하단 액션을 viewport에 고정하고, 상품
  section만 내부 스크롤되도록 높이 체인을 잠금. 마지막 구매 저장 성공 시 같은 화면에서 한 번만
  가방 점프와 결정적 색종이 팡파레를 재생하며 초기 완료·필터·재검증·rollback에는 재생하지 않고
  reduced-motion에서는 정적 완료 상태만 제공
- v6.20 · 여행 상태 배지를 요약 카드에서 앱 헤더 제목 오른쪽으로 이동. 예정·여행 중·완료 요약을
  `왼쪽 수치 · 중앙 가방 · 오른쪽 수치`의 한 stage로 통일하고 가방 내부만 packing/transit/unpacking
  장면으로 변형. 100% 미만 예산의 반올림 오류를 없애 14,090/14,160을 99.5%로 고정하고, 예산 편집
  행·툴바·88px 상품 행을 압축해 짧은 화면에서 한 상품 행 이상을 완전히 노출
- v6.21 · Hallmark 재설계. 상태 요약을 큰 중앙 가방과 하단 status rail의
  `central-instrument task console`로 재구성하고 packing/transit/unpacking의 의미를 강화. 예정 예산은
  저장 버튼 없는 즉시 preview+450ms 자동 저장과 총 예상 비용에 붙은 세로 reservoir로 연결. 예정의
  구매 상태 필터를 제거하고 얇은 검색+단일 행 정렬 탭, 암전+가로 취소선 상품 행으로 교체. 같은 화면의
  미완료→전체 완료 저장 성공 전환마다 가방 점프와 결정적 팡파레를 다시 재생하도록 교정
- v6.22 · 여권 도장 자리표를 등간격·두 열에서 **불규칙 배치**로 교체. 세로 간격(18/26/17 등),
  좌우(왼쪽 0–3·오른쪽 34–38), 자리별 기울기(−9°~+9°)를 함께 흩고 여행별 흔들림은 ±2°로 축소.
  이웃 단 겹침 한도 20% → 30%, 먼 쌍 무겹침은 유지
- v6.23 · 도장 각도·겹침을 EU `Practical Handbook for Border Guards`(Schengen Borders Code
  부속서 IV 실무 지침)에 맞춰 교정. 기울기 −17°~+15° → **−6°~+5°**(가로 방향 권고), 이웃 단
  겹침 30% → 14%, 먼 쌍 0%. 나라별 기본 각도도 절반으로 낮추고 칸을 62%→55%로 좁혀
  크기는 유지(작은 화면 +2%, 큰 화면 −8%). 불규칙은 각도가 아니라 **간격**으로만 만든다
- v6.24 · 홈 하단 고정 여행 도크와 여행 전환 시트를 제거. 여행 선택·전환·생성은 여행 탭이
  단독으로 맡고 홈은 활성 여행의 쇼핑 컨텍스트만 보여 준다. 도크 자리를 비우던 하단 여백도 삭제
- v6.25 · Hallmark 재감사로 래스터·클레이 가방을 폐기하고 기존 선화를 연결된 앞·상단·우측 면의
  line-volume 장면으로 확장. 같은 stroke·bevel·side-face의 대형 좌측 HP와 Toss형 연속 surface를
  고정하고, 예정 정렬은 독립 버튼, 상품 삭제는 왼쪽 swipe reveal로 교정
- v6.25 · 홈 체크리스트를 목록 중심으로 재편. `직접 추가 / 사진으로 추가` 두 버튼을 제목 줄
  오른쪽 원형 카메라 버튼 하나로 대체하고, 섹션 제목 18px→13px·필터 칩 44px→28px(터치 44px 유지),
  미리보기 3줄→4줄. 직접 추가는 여행 상세가 단독으로 맡는다
- v6.26 · 예전 디자인의 용어·밀도를 복원. 섹션 이름을 모드 무관 `쇼핑리스트`로 통일하고
  상품 행은 금액·수량 한 줄(일차 제거)·썸네일 48→40px로 압축.
  사진 추가 시트의 고르기 단계 문구를 네 흐름 공통(`사진으로 추가` · `디토 AI 분석 시작`)으로
  정리하고 `살 것 후보` 계열 표현을 전부 제거. 헤더 패딩도 좁힘
- v6.27 · 홈 쇼핑리스트의 사진 추가를 아이콘 전용 원형 버튼에서 **글자 있는 채움 버튼**
  (`사진으로 추가`)으로 교체. 아이콘만으로는 무엇이 추가되는지 알 수 없어 눌러 봐야 했다
- v6.28 · 사진 추가 버튼을 `+` + 카메라 두 글리프에서 **렌즈에 `+`가 들어간 단일 아이콘**
  (자체 제작)으로 교체하고 28px 원형으로 축소. 제목·버튼·칩을 28px 격자에 정렬
- v6.29 · 추가 아이콘을 Material `add_a_photo`·SF Symbols `camera.badge.plus` 관례에 맞춰
  교정. `+`는 본체를 대체하지 않고 **오른쪽 위 배지**로 붙이며 렌즈는 유지, 아이콘 16→18px
- v6.30 · 사진 추가 버튼 28→36px, 아이콘 18→20px. 칩 높이에 맞춘 28px은 표준 아이콘
  버튼보다 작아 눌러도 되는 것으로 읽히지 않았다. 터치 영역은 44px 유지
- v6.31 · 쇼핑리스트 제목 13→17px(개수 배지 12→13px), 상단 패딩 8→4px. 36px 추가 버튼과
  무게를 맞추고, 글자를 키운 만큼 줄 안 여백이 줄어 제목 위 간격이 18→12px로 좁아졌다
- v6.32 · 상품 추가를 카드 헤더 버튼에서 **화면 오른쪽 아래 FAB**(56px)로 이동. 카드에는
  제목만 남아 줄 높이가 버튼 크기에서 풀렸고, FAB가 뜨는 화면은 본문 아래 여백을 72px 더한다
- v6.33 · 홈에서 활성 여행을 바꿀 방법이 없던 문제 수정. 대문의 도시 이름을 `도시 ⌄` 버튼으로
  만들어 여행 전환 시트를 연다(여행 2개 이상일 때만). 카드 링크는 `absolute inset-0`으로 내리고
  본문을 `pointer-events-none`으로 통과시켜 `<a>` 안 `<button>` 중첩을 피했다
- v6.34 · 홈 쇼핑리스트 필터 줄을 모드별 중복 마크업에서 **단일 스크롤러 + 데이터 분기**로
  통합. 여행 중에만 빠져 있던 드래그 후 클릭 차단이 살아나고 스크롤바 숨김 방식도 일치.
  빈 상태 문구를 모드 무관으로 정리하고, FAB 이전 후 남아 있던 `위 카메라로` 안내를 교정
- v6.35 · 홈 쇼핑리스트 필터를 모드 무관 **일차 하나**로 통일(구매 상태 필터 삭제),
  `더보기` 버튼을 **카드 내부 스크롤 + 8개씩 무한 로딩**으로 교체. 대문 링크를 여행 중에도
  상세로 통일하고, 여행 상태 배지를 `TripStatusBadge` 하나로 합쳐 여행 탭과 전환 시트를 일치
- v6.36 · 여권 화면을 **닫힌 표지**로 시작하도록 변경. 표지를 누르면 제본선을 축으로 105°
  젖혀지며 내지가 나온다. 닫힌 동안 내지는 `inert`, 도장 찍기 진입은 표지를 건너뛴다
- v6.37 · 표지 열기를 CSS transition에서 **쪽 넘김과 동일한 rAF 구동**으로 교체(곡면 음영·
  캐스트 섀도·340ms 공유). 진입 후 520ms 뒤 자동으로 넘어가며, 탭하면 즉시 열린다
- v6.38 · 표지에 뒷면을 추가해 쪽 넘김과 같은 두께감을 갖게 하고, 자동 열기를 되돌려
  `눌러서 펼치기` 숨쉬기 힌트로 교체. 도장 찍기 진입만 표지→목표 장까지 한 장씩 자동 진행
